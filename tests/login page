import { test, expect } from '@playwright/test';

test.describe('Create Lead Suite', () => {

  test('Create a new lead successfully', async ({ page }) => {

    await test.step('Perform login', async () => {
      await page.goto('https://www.instagram.com/accounts/login/?hl=en');

      await page.locator('input[name="username"]').fill('sudarshanbhatia');
      await page.locator('input[name="password"]').fill('Sudarshan@123');
      await page.locator('button[type="submit"]').click();
        });    // closes test.step
  });      // closes test
});  